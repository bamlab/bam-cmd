#!/usr/bin/env node

require('colors');


var bamCmd = require('..');
bamCmd.loadConfig()
	.then(function(configManager) {
		return configManager.runInstall({});
	})
	.catch(function (err) {
		console.error((err + '').red);
		console.error('Abording.'.red);
		process.exit(1);
	})
;
