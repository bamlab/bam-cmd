var config = {
  scriptVersion: '0.0.234987689',
  dirName: 'strings-fixture-config',
  linkedRepos: [],
  install: 'ls',
  postInstall: 'ls -al',
  build: 'ls -al > out',
  deploy: 'deploy',
};

module.exports =  config;
